import 'package:flutter/material.dart';

class AppColors {
  /// Brand primary color (from Figma)
  static const Color primaryOrange = Color(0xFFFF5500);

  /// Common colors
  static const Color white = Colors.white;
}
